# Web App de Google Apps Script (recomendado para el MVP).
# Publica el script como aplicación web y pega la URL aquí.
GOOGLE_SHEETS_WEBHOOK_URL=

# Si esta variable está vacía, los registros se guardan en .data/registrations.json
# (útil en local). En producción conviene siempre configurar la hoja.
